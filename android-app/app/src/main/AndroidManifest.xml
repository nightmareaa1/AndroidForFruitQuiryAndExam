<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".UserAuthApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_vector"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_vector"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserAuth"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- API Base URL placeholder (配置在 build.gradle 的 manifestPlaceholders 中) -->
        <meta-data
            android:name="API_BASE_URL"
            android:value="${API_BASE_URL}" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.UserAuth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>