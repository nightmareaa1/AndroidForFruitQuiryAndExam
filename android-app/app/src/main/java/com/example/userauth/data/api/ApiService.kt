package com.example.userauth.data.api

import retrofit2.Response
import retrofit2.http.*

/**
 * API service interface for backend communication
 * This will be expanded with specific endpoints in later tasks
 */
interface ApiService {
    // TODO: Add API endpoints for authentication, competitions, and fruit queries
}